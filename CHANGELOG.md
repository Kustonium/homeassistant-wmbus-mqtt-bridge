## 1.4.4

### PL

- Zaktualizowano listę dostępnych driverów `type` dla liczników.
- Lista została dopasowana do aktualnego `[edge] Wmbusmeters (W-MBus to MQTT)` / `wmbusmeters-ha-addon-edge` w wersji `2.0.0-372`.
- Dodano nowe / aktualne drivery z gałęzi edge, m.in. `kamwater`, `zennerhca`, `caltose`, `c5isf`, `rfmamb`, `qheatus`.
- Zachowano starsze wpisy używane wcześniej w konfiguracjach, m.in. `flowiq2200`, `multical21`, `hydrodigit`, `qheat`, `qwater`, aby nie zablokować istniejących konfiguracji użytkowników.

### EN

- Updated the available meter `type` driver list.
- The list is now aligned with the current `[edge] Wmbusmeters (W-MBus to MQTT)` / `wmbusmeters-ha-addon-edge` version `2.0.0-372`.
- Added new / current edge drivers, including `kamwater`, `zennerhca`, `caltose`, `c5isf`, `rfmamb`, `qheatus`.
- Kept older entries previously used in configurations, including `flowiq2200`, `multical21`, `hydrodigit`, `qheat`, `qwater`, to avoid breaking existing user setups.